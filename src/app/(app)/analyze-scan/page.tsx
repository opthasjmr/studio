
import { AnalyzeScanForm } from "@/components/analyze/AnalyzeScanForm";

export default function AnalyzeScanPage() {
  // The AnalyzeScanForm itself has a container with padding.
  // If additional padding is needed for the page level, it can be added here.
  // For now, assuming AnalyzeScanForm handles its own layout within the main content area.
  return <AnalyzeScanForm />;
}
